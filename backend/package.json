{
  "name": "booking-system-backend",
  "version": "1.0.0",
  "description": "Backend API for rental booking system",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/init-db.js && node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No automated tests are configured for the backend\"",
    "db:migrate": "node scripts/init-db.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.3.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
